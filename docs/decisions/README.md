# Architecture decision records

Important implementation choices will be recorded here as short Architecture
Decision Records (ADRs). The first ADR should select the SSR framework,
deployment topology, and matching persistent usage store after those options are
evaluated together.
