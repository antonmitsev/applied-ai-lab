# Shared packages

Reusable code belongs here only after at least two applications need the same
capability. The initial Plumbing Assistant should keep app-specific code within
its own directory until a genuine shared abstraction emerges.
